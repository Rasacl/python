"""
相同点：
   进程和线程都能够完成多任务处理的能力，都可以提高程序的执行效率。
区别：
   (1)进程之间互相独立，而线程之间则共享内存，所以进程之间通信较线程之间通信困难。
   (2)进程有独立的虚拟内存空间，而线程没有独立的虚拟内存空间。
   (3)进程在执行过程中，可以主动创建和撤销线程，也可以挂起和恢复线程，而线程不能主动结束或终止。
   (4)进程间通信较线程间通信复杂，而线程间通信简单。
   (5)进程间通信的实现方法有：管道、FIFO、Socket、消息队列、信号量、共享内存、网络等，而线程间通信的实现方法只有：互斥锁、条件变量、信号量、事件。
   (6)进程切换开销大，而线程切换开销小。
   (7)一个线程挂掉，不会影响其他线程，进程挂掉，会导致进程挂掉。
   (8)进程在执行过程中，可以随时创建和删除线程，而线程在执行过程中，不能创建或删除线程。
   (9)进程是操作系统资源分配的最小单位，而线程是操作系统调度的最小单位。
   (10)一个线程可以创建另一个线程或进程，而进程创建进程或线程是不允许的。
   (11)一个进程内的多个线程共享代码段和数据段，而不同的进程之间是相互独立的。
"""